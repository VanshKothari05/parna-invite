import RevealOnScroll from '../components/RevealOnScroll';

export default function AathaiIntro() {
  return (
    <section className="section intro">
      <RevealOnScroll>
        <p className="intro__text">
          Eight days of tapasya, held with quiet discipline and devotion, now find
          their gentle completion. A moment of Parna — of purity meeting
          celebration — and we would be honoured to have you share it with us.
        </p>
      </RevealOnScroll>
    </section>
  );
}
